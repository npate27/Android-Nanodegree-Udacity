<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >
    <TextView
        android:id="@+id/trailer_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/height_trailer_title"
        android:layout_marginEnd="@dimen/margin_end_trailer_height"
        android:layout_marginStart="@dimen/margin_start_trailer_title"
        app:layout_constraintBottom_toBottomOf="@+id/play_iv"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/play_iv"
        app:layout_constraintTop_toTopOf="@+id/play_iv"
        tools:text="Movie Trailer Final Trailer" />

    <ImageView
        android:id="@+id/play_iv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_bottom_play"
        android:layout_marginStart="@dimen/margin_start_play"
        android:layout_marginTop="@dimen/margin_top_play"
        android:contentDescription="@string/trailer_item_iv_description"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@android:drawable/ic_media_play" />

</android.support.constraint.ConstraintLayout>